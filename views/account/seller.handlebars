<section class="account">
  <header class="account-header">
    <h1>Kênh người bán</h1>
    {{#if seller}}
      <div class="user-card">
        <h2>{{seller.name}}</h2>
        <p>Email: {{seller.email}}</p>
        <p>Đánh giá: {{seller.ratingPlus}} 👍 / {{seller.ratingMinus}} 👎</p>
      </div>
    {{/if}}
  </header>

  <section class="account-section">
    <h2>Sản phẩm đang đăng</h2>
    {{#if activeProducts.length}}
      <div class="product-grid">
        {{#each activeProducts}}
          {{> product-card}}
        {{/each}}
      </div>
    {{else}}
      <p>Chưa có sản phẩm nào đang đấu giá.</p>
    {{/if}}
  </section>

  <section class="account-section">
    <h2>Sản phẩm đã hoàn tất</h2>
    {{#if completedProducts.length}}
      <div class="product-grid">
        {{#each completedProducts}}
          {{> product-card}}
        {{/each}}
      </div>
    {{else}}
      <p>Chưa có giao dịch hoàn tất.</p>
    {{/if}}
  </section>

  <section class="account-section">
    <h2>Cài đặt gia hạn tự động</h2>
    <ul class="settings-list">
      <li>Kích hoạt khi còn {{autoExtendSettings.triggerMinutes}} phút cuối</li>
      <li>Gia hạn thêm {{autoExtendSettings.extendMinutes}} phút</li>
    </ul>
  </section>
</section>
